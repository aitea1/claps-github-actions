version: "3.8"

services:
  dir1:
    build:
      context: .
      dockerfile: Docker/dockerfile
    container_name: clasp-github-actions
    volumes:
      - ./clasp-github-actions/app:/app
    tty: true
